# Narro Frontend Server - Production Environment Configuration
# Copy this file to /home/narro/deployment/.env.production and fill in your actual values
# Set permissions: chmod 600 .env.production
# This file is for FRONTEND servers only (runs Next.js web application)

# Container Registry Configuration
REGISTRY_URL=ord.vultrcr.com/narro
REGISTRY_USER=your-registry-username
REGISTRY_PASSWORD=your-registry-password
IMAGE_TAG=latest

# Application Configuration
NODE_ENV=production
PORT=3001

# API Backend URL
# Frontend needs to know where to reach the API backend server
# Default: api.narro.info (must match backend server domain)
NEXT_PUBLIC_API_URL=https://api.narro.info

# Supabase Configuration (for client-side authentication)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# S3 Storage (for displaying thumbnails from bucket)
NEXT_PUBLIC_S3_BASE_URL=https://your-bucket-name.region.digitaloceanspaces.com

# Optional: Sentry Error Tracking (frontend)
# NEXT_PUBLIC_SENTRY_DSN=https://your-frontend-sentry-dsn@sentry.io/frontend-project-id
# NEXT_PUBLIC_SENTRY_RELEASE=narro-web@1.0.0
